# .env.local.example — copy to .env.local and fill in values
NEXT_PUBLIC_INSTANT_APP_ID=   # client-side (bundled into browser JS)
INSTANT_APP_ID=               # same value — server-only alias (not bundled)
INSTANT_ADMIN_TOKEN=
NEXT_PUBLIC_MAPBOX_TOKEN=
NREL_API_KEY=
GEOAPIFY_API_KEY=
CRON_SECRET=
